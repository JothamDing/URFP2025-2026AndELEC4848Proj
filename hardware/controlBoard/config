# Smoothieboard configuration file, see http://smoothieware.org/configuring-smoothie
# NOTE Lines must not exceed 132 characters, and '#' characters mean what follows is ignored
## Robot module configurations : general handling of movement G-codes and slicing into moves
#Modify by Ding Zhouchen   
#Supervisor: Prof. X. Qi


# Basic motion configuration
default_feed_rate                            5000             # Default speed (mm/minute) for G1/G2/G3 moves
default_seek_rate                            5000             # Default speed (mm/minute) for G0 moves
mm_per_arc_segment                           0.0              # Fixed length for line segments that divide arcs, 0 to disable
#mm_per_line_segment                         5                # Cut lines into segments this size
mm_max_arc_error                             0.01             # The maximum error for line segments that divide arcs 0 to disable
                                                              # note it is invalid for both the above be 0
                                                              # if both are used, will use largest segment length based on radius

# Arm solution configuration : Cartesian robot. Translates mm positions into stepper positions
microseconds_per_step_pulse                    1
base_stepping_frequency                          	100000
arm_solution       corexy

# See http://smoothieware.org/stepper-motors
alpha_steps_per_mm                           160               # Steps per mm for alpha ( X ) stepper
beta_steps_per_mm                            160        # Steps per mm for beta ( Y ) stepper
gamma_steps_per_mm                       160             # Steps per mm for gamma ( Z ) stepper

# Planner module configuration : Look-ahead and acceleration configuration
# See http://smoothieware.org/motion-control
acceleration                                 1000             # Acceleration in mm/second/second.
z_acceleration                              400              # Acceleration for Z only moves in mm/s^2, 0 uses acceleration which is the default. DO NOT SET ON A DELTA
junction_deviation                           0.05             # See http://smoothieware.org/motion-control#junction-deviation
#z_junction_deviation                        0.0              # For Z only moves, -1 uses junction_deviation, zero disables junction_deviation on z moves DO NOT SET ON A DELTA

# Cartesian axis speed limits
x_axis_max_speed                             30000            # Maximum speed in mm/min
y_axis_max_speed                             30000            # Maximum speed in mm/min
z_axis_max_speed                             5000              # Maximum speed in mm/min

# Stepper module configuration
# Pins are defined as  ports, and pin numbers, appending "!" to the number will invert a pin
# See http://smoothieware.org/pin-configuration and http://smoothieware.org/pinout
alpha_step_pin                               2.0              # Pin for alpha stepper step signal
alpha_dir_pin                                0.5              # Pin for alpha stepper direction, add '!' to reverse direction
alpha_en_pin                                 0.4              # Pin for alpha enable pin
alpha_current                                1.5              # X stepper motor current
alpha_max_rate                               30000.0          # Maximum rate in mm/min

beta_step_pin                                2.1              # Pin for beta stepper step signal
beta_dir_pin                                 0.11!             # Pin for beta stepper direction, add '!' to reverse direction
beta_en_pin                                  0.10             # Pin for beta enable
beta_current                                 1.5              # Y stepper motor current
beta_max_rate                                30000.0          # Maxmimum rate in mm/min

gamma_step_pin                               2.2              # Pin for gamma stepper step signal
gamma_dir_pin                                0.20             # Pin for gamma stepper direction, add '!' to reverse direction
gamma_en_pin                                 0.19             # Pin for gamma enable
gamma_current                                1.5              # Z stepper motor current
gamma_max_rate                               2000.0            # Maximum rate in mm/min

# A axis
delta_steps_per_mm                    17.7777     # may be steps per degree for example
delta_step_pin                        2.3      # Pin for delta stepper step signal
delta_dir_pin                         0.22      # Pin for delta stepper direction
delta_en_pin                          0.21      # Pin for delta enable

delta_max_rate                       10000   # mm/min
delta_acceleration                    10000.0   # mm/sec²

# B axis
epsilon_steps_per_mm                  17.7777     # may be steps per degree for example
epsilon_step_pin                      2.8      # Pin for delta stepper step signal
epsilon_dir_pin                       2.13      # Pin for delta stepper direction
epsilon_en_pin                        4.29      # Pin for delta enable

epsilon_max_rate                      10000.0   # mm/min
epsilon_acceleration                  10000.0   # mm/sec²


## Switch modules
# See http://smoothieware.org/switch

# Switch module for fan control
switch.fan.enable                            true             # Enable this module
switch.fan.input_on_command                  M106             # Command that will turn this switch on
switch.fan.input_off_command                 M107             # Command that will turn this switch off
switch.fan.output_pin                        2.6              # Pin this module controls
switch.fan.output_type                       digital              # PWM output settable with S parameter in the input_on_comand

# Switch module for pumpA control
switch.pumpA.enable                            true             # Enable this module
switch.pumpA.input_on_command                  M34             # Command that will turn this switch on
switch.pumpA.input_off_command                 M35             # Command that will turn this switch off
switch.pumpA.output_pin                        1.23              # Pin this module controls
switch.pumpA.output_type                       digital              # PWM output settable with S parameter in the input_on_comand

# Switch module for blowPump control
switch.pumpB.enable                            true             # Enable this module
switch.pumpB.input_on_command                  M36             # Command that will turn this switch on
switch.pumpB.input_off_command                 M37             # Command that will turn this switch off
switch.pumpB.output_pin                        2.5              # Pin this module controls
switch.pumpB.output_type                       digital              # PWM output settable with S parameter in the input_on_comand

# Switch module for mos3 control
switch.mos3.enable                            false             # Enable this module
switch.mos3.input_on_command                  M38             # Command that will turn this switch on
switch.mos3.input_off_command                 M39             # Command that will turn this switch off
switch.mos3.output_pin                        2.7^              # Pin this module controls
switch.mos3.output_type                       digital              # PWM output settable with S parameter in the input_on_comand

# Switch module for moss1 control
switch.moss1.enable                            true             # Enable this module
switch.moss1.input_on_command                  M40             # Command that will turn this switch on
switch.moss1.input_off_command                 M41             # Command that will turn this switch off
switch.moss1.output_pin                        2.4              # Pin this module controls
switch.moss1.output_type                       digital              # PWM output settable with S parameter in the input_on_comand

# Switch module for moss2 control
switch.moss2.enable                            true             # Enable this module
switch.moss2.input_on_command                  M42             # Command that will turn this switch on
switch.moss2.input_off_command                 M43             # Command that will turn this switch off
switch.moss2.output_pin                        1.22              # Pin this module controls
switch.moss2.output_type                       digital              # PWM output settable with S parameter in the input_on_comand

#LIGHT1
switch.light1.enable                            true             # Enable this module
switch.light1.input_on_command                  M44             # M280 S7.5 would be midway
switch.light1.input_off_command                 M45             # Same as M280 S0 0% duty cycle, effectively off
switch.light1.output_pin                        3.25             # May be any spare pin
switch.light1.output_type                       digital            # Software pwm output settable with S parameter in the input_on_command
#switch.light1.pwm_period_ms                    20               # set period to 20ms (50Hz) default is 50Hz
#switch.light1.startup_state                    false            # false uses startup_value on boot true uses default_on_value
#switch.light1.startup_value                    0             # On boot and HALT it will set this PWM value
#switch.light1.default_on_value                 3.3              # This PWM value will be set if M280 doe snot have an S parameter, it is also the value used if startup_state is true

#LIGHT2
switch.light2.enable                            true             # Enable this module
switch.light2.input_on_command                  M46             # M280 S7.5 would be midway
switch.light2.input_off_command                 M47             # Same as M280 S0 0% duty cycle, effectively off
switch.light2.output_pin                        3.26             # May be any spare pin
switch.light2.output_type                       digital            # Software pwm output settable with S parameter in the input_on_command


## Endstops
# See http://smoothieware.org/endstops
endstops_enable                              true             # The endstop module is enabled by default and can be disabled here
corexy_homing                               true            # Set to true if homing on a hbot or corexy
alpha_min_endstop                            1.24^            # Pin to read min endstop, add a ! to invert if endstop is NO connected to ground
#alpha_max_endstop                           1.25^            # Pin to read max endstop, uncomment this and comment the above if using max endstops
alpha_homing_direction                       home_to_min      # Or set to home_to_max and set alpha_max and uncomment the alpha_max_endstop
alpha_min                                    0                # This gets loaded as the current position after homing when home_to_min is set
alpha_max                                    260              # This gets loaded as the current position after homing when home_to_max is set
#beta_min_endstop                             1.26^            # Pin to read min endstop, add a ! to invert if endstop is NO connected to ground
beta_max_endstop                            1.27^            # Pin to read max endstop, uncomment this and comment the above if using max endstops
beta_homing_direction                        home_to_max      # Or set to home_to_max and set alpha_max and uncomment the alpha_max_endstop
beta_min                                     0                # This gets loaded as the current position after homing when home_to_min is set
beta_max                                     290              # This gets loaded as the current position after homing when home_to_max is set
gamma_min_endstop                            1.28^            # Pin to read min endstop, add a ! to invert if endstop is NO connected to ground
#gamma_max_endstop                           1.29^            # Pin to read max endstop, uncomment this and comment the above if using max endstops
gamma_homing_direction                       home_to_min      # Or set to home_to_max and set alpha_max and uncomment the alpha_max_endstop
gamma_min                                    -33                # This gets loaded as the current position after homing when home_to_min is set
gamma_max                                    67              # This gets loaded as the current position after homing when home_to_max is set

alpha_max_travel                             300              # Max travel in mm for alpha/X axis when homing
beta_max_travel                              360              # Max travel in mm for beta/Y axis when homing
gamma_max_travel                             80              # Max travel in mm for gamma/Z axis when homing

# Optional enable limit switches, actions will stop if any enabled limit switch is triggered
alpha_limit_enable                          false            # Set to true to enable X min and max limit switches
beta_limit_enable                           false            # Set to true to enable Y min and max limit switches
gamma_limit_enable                          false            # Set to true to enable Z min and max limit switches

# Endstops home at their fast feedrate first, then once the endstop is found they home again at their slow feedrate for accuracy
alpha_fast_homing_rate_mm_s                  50              # Alpha/X fast homing feedrate in mm/second
alpha_slow_homing_rate_mm_s                  20               # Alpha/X slow homing feedrate in mm/second
beta_fast_homing_rate_mm_s                   50               # Beta/Y  fast homing feedrate in mm/second
beta_slow_homing_rate_mm_s                   20               # Beta/Y  slow homing feedrate in mm/second
gamma_fast_homing_rate_mm_s                  60                # Gamma/Z fast homing feedrate in mm/second
gamma_slow_homing_rate_mm_s                  15                # Gamma/Z slow homing feedrate in mm/second

alpha_homing_retract_mm                      5                # Distance to retract from the endstop after it is hit for alpha/X
beta_homing_retract_mm                       5               # Distance to retract from the endstop after it is hit for beta/Y
gamma_homing_retract_mm                      2                # Distance to retract from the endstop after it is hit for gamma/Z

# Optional order in which axis will home, default is they all home at the same time,
# If this is set it will force each axis to home one at a time in the specified order
homing_order                                 ZXY              # X axis followed by Y then Z last
move_to_origin_after_home                    true            # Move XY to 0,0 after homing
#endstop_debounce_count                       100              # Uncomment if you get noise on your endstops, default is 100
#endstop_debounce_ms                          1                # Uncomment if you get noise on your endstops, default is 1 millisecond debounce
home_z_first                                 true             # Uncomment and set to true to home the Z first, otherwise Z homes after XY

#softlimit
soft_endstop.enable         false         # Enable soft endstops
soft_endstop.x_min          0            # Minimum X position
soft_endstop.x_max          260          # Maximum X position
soft_endstop.y_min          0            # Minimum Y position
soft_endstop.y_max          290          # Maximum Y position
soft_endstop.z_min          -33            # Minimum Z position
soft_endstop.z_max          67          # Maximum Z position
soft_endstop.halt           true         # Whether to issue a HALT state when hitting a soft endstop (if false, will just ignore commands that would exceed the limit)


# End of endstop config
# Delete the above endstop section and uncomment next line and copy and edit Snippets/abc-endstop.config file to enable endstops for ABC axis
#include abc-endstop.config


## Custom menus : Example of a custom menu entry, which will show up in the Custom entry.
# NOTE _ gets converted to space in the menu and commands, | is used to separate multiple commands
custom_menu.power_on.enable                true              #
custom_menu.power_on.name                  Power_on          #
custom_menu.power_on.command               M80               #

custom_menu.power_off.enable               true              #
custom_menu.power_off.name                 Power_off         #
custom_menu.power_off.command              M81               #



## System configuration
# Serial communications configuration ( baud rate defaults to 9600 if undefined )
# For communication over the UART port, *not* the USB/Serial port
uart0.baud_rate                              115200           # Baud rate for the default hardware ( UART ) serial port

second_usb_serial_enable                     true            # This enables a second USB serial port
#leds_disable                                true             # Disable using leds after config loaded
#play_led_disable                            true             # Disable the play led

# Kill button maybe assigned to a different pin, set to the onboard pin by default
# See http://smoothieware.org/killbutton
kill_button_enable                           true             # Set to true to enable a kill button
kill_button_pin                              2.12             # Kill button pin. default is same as pause button 2.12 (2.11 is another good choice)

#msd_disable                                 false            # Disable the MSD (USB SDCARD), see http://smoothieware.org/troubleshooting#disable-msd
#dfu_enable                                  false            # For linux developers, set to true to enable DFU

# Only needed on a smoothieboard
# See http://smoothieware.org/currentcontrol
currentcontrol_module_enable                 false             # Control stepper motor current via the configuration file
